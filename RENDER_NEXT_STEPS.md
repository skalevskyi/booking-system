# 🚀 Наступні кроки на Render Dashboard

## Крок 1: Переконайтеся, що код оновлено на GitHub

Спочатку закомітьте та відправте виправлення на GitHub:

```powershell
git add .
git commit -m "Fix TypeScript compilation errors for Render deployment"
git push
```

## Крок 2: На Render Dashboard

### A. Якщо деплой ще не завершено або не вдався:

1. **Перевірте логи деплою:**
   - Натисніть на вкладку **"Logs"** у верхній частині сторінки
   - Перевірте, чи є помилки компіляції
   - Якщо помилки є, переконайтеся, що код оновлено на GitHub

2. **Запустіть Manual Deploy:**
   - Натисніть кнопку **"Manual Deploy"** → **"Deploy latest commit"**
   - Або дочекайтеся автоматичного redeploy після push до GitHub

### B. Якщо деплой успішний:

1. **Перевірте Environment Variables:**
   - Перейдіть до вкладки **"Environment"**
   - Переконайтеся, що всі змінні налаштовані:
     - `NODE_ENV=production`
     - `PORT=10000` (або порт, який надав Render)
     - `DATABASE_URL` (має бути автоматично підключено з бази даних)
     - `JWT_SECRET` (має бути згенеровано автоматично)
     - `JWT_REFRESH_SECRET` (має бути згенеровано автоматично)
     - `JWT_ACCESS_EXPIRES_IN=15m`
     - `JWT_REFRESH_EXPIRES_IN=7d`
     - `FRONTEND_URL=https://skalevskyi.github.io`

2. **Виконайте міграції бази даних:**
   - Натисніть на вкладку **"Shell"** (або **"Shell"** у меню)
   - Виконайте команди:
   ```bash
   cd apps/api
   pnpm prisma:migrate deploy
   pnpm tsx prisma/seed.ts
   ```

3. **Перевірте, чи сервіс працює:**
   - Натисніть на **URL сервісу** (вгорі сторінки, типу `https://booking-system-api.onrender.com`)
   - Або перевірте health endpoint: `https://your-service-url.onrender.com/health`
   - Має повернутися: `{"status":"ok"}`

4. **Перевірте Swagger UI:**
   - Відкрийте: `https://your-service-url.onrender.com/docs`
   - Має відкритися Swagger документація

## Крок 3: Оновіть Frontend

Після того, як backend працює:

1. **Скопіюйте URL вашого Render сервісу** (наприклад: `https://booking-system-api.onrender.com`)

2. **Додайте Secret у GitHub:**
   - Перейдіть до вашого GitHub репозиторію
   - Settings → Secrets and variables → Actions
   - Додайте або оновіть `VITE_API_URL` з URL вашого Render сервісу

3. **Запустіть redeploy frontend:**
   - GitHub Actions автоматично задеплоїть frontend з новим API URL
   - Або натисніть "Re-run jobs" у GitHub Actions

## Troubleshooting

### Якщо деплой не вдається:

1. **Перевірте логи:**
   - Вкладка **"Logs"** покаже детальні помилки
   - Переконайтеся, що всі environment variables налаштовані

2. **Перевірте build команду:**
   - У **Settings** → **Build Command** має бути:
   ```
   corepack enable && corepack prepare pnpm@latest --activate && pnpm install --frozen-lockfile && pnpm approve-builds bcrypt @prisma/client prisma && pnpm prisma:generate && pnpm build
   ```

3. **Перевірте start команду:**
   - У **Settings** → **Start Command** має бути:
   ```
   pnpm start
   ```

4. **Перевірте Root Directory:**
   - У **Settings** → **Root Directory** має бути:
   ```
   apps/api
   ```

### Якщо база даних не підключається:

1. Перевірте, що база даних створена
2. Перевірте, що `DATABASE_URL` правильно налаштований
3. Перевірте, що база даних та сервіс в одному регіоні

### Якщо міграції не виконуються:

1. Виконайте міграції вручну через Shell
2. Переконайтеся, що Prisma Client згенеровано (`pnpm prisma:generate`)

---

**Після успішного деплою ваш backend буде доступний за URL, який надав Render!** 🎉

