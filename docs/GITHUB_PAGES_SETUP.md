# 🔧 Налаштування GitHub Pages

Якщо на сайті `https://skalevskyi.github.io/booking-system/` відкривається README.md замість React додатку, виконайте наступні кроки:

## Крок 1: Перевірте налаштування GitHub Pages

1. Перейдіть до вашого репозиторію на GitHub
2. Відкрийте **Settings** → **Pages**
3. У розділі **Source** переконайтеся, що вибрано:
   - **Source**: `GitHub Actions` (НЕ `Deploy from a branch`)
4. Якщо вибрано інший варіант, змініть на `GitHub Actions` та збережіть

## Крок 2: Перевірте, чи є Secret VITE_API_URL

1. Перейдіть до **Settings** → **Secrets and variables** → **Actions**
2. Переконайтеся, що є secret з назвою `VITE_API_URL`
3. Якщо його немає, додайте:
   - **Name**: `VITE_API_URL`
   - **Value**: URL вашого backend API (наприклад, `https://your-api.onrender.com`)

## Крок 3: Запустіть workflow вручну

1. Перейдіть до **Actions** у вашому репозиторії
2. Знайдіть workflow "Deploy Web to GitHub Pages"
3. Натисніть **Run workflow** → **Run workflow** (з гілки main)
4. Дочекайтеся завершення workflow (обидва jobs мають бути зеленими)

## Крок 4: Перевірте результат

Після успішного виконання workflow:

1. Перейдіть до **Settings** → **Pages**
2. Перевірте, що вказано URL вашого сайту
3. Відкрийте `https://skalevskyi.github.io/booking-system/` у браузері
4. Має відкритися React додаток (сторінка Login/Register), а не README.md

## Troubleshooting

### Workflow не запускається

- Перевірте, що файл `.github/workflows/deploy-web.yml` існує
- Перевірте, що ви знаходитесь на гілці `main`
- Спробуйте зробити будь-який commit та push до `main`

### Workflow падає з помилкою

1. Перейдіть до **Actions** → виберіть failed workflow run
2. Перевірте логи, особливо:
   - Чи встановлюються залежності
   - Чи проходить build
   - Чи є помилки в deploy job

### Сайт все ще показує README.md

1. Перевірте, що GitHub Pages налаштований на `GitHub Actions` (не на гілку)
2. Перевірте, що workflow виконався успішно
3. Очистіть кеш браузера (Ctrl+Shift+R або Cmd+Shift+R)
4. Спробуйте відкрити в режимі інкогніто

### 404 помилка на підсторінках

Це нормально для SPA. Переконайтеся, що:
- У workflow є крок "SPA fallback" (копіювання index.html в 404.html)
- У `vite.config.ts` встановлено `base: '/booking-system/'`

## Якщо нічого не допомагає

1. Видаліть налаштування GitHub Pages (Settings → Pages → Source: None)
2. Збережіть
3. Знову налаштуйте на `GitHub Actions`
4. Запустіть workflow вручну

---

**Важливо:** Після будь-яких змін у workflow або налаштуваннях GitHub Pages, дочекайтеся завершення workflow перед перевіркою сайту.

