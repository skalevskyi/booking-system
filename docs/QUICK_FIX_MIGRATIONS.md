# 🔧 Швидке виправлення: Міграції не виконані

Якщо ви бачите помилку `The table public.users does not exist`, виконайте наступні кроки:

## Варіант 1: Ручний перезапуск на Render (найшвидше)

1. Перейдіть до [Render Dashboard](https://dashboard.render.com)
2. Виберіть ваш сервіс `booking-system-api`
3. Натисніть кнопку **Manual Deploy** → **Deploy latest commit**
4. Дочекайтеся завершення деплою (2-3 хвилини)
5. Під час деплою перевірте логи - має з'явитися повідомлення про виконання міграцій

## Варіант 2: Перезапуск через зміну змінної середовища

1. Перейдіть до Render Dashboard → ваш сервіс
2. Відкрийте вкладку **Environment**
3. Додайте або змініть будь-яку змінну (наприклад, додайте пробіл до `FRONTEND_URL`)
4. Натисніть **Save Changes**
5. Render автоматично перезапустить сервіс з новим `startCommand`

## Варіант 3: Push нового commit

Якщо зміни в `render.yaml` ще не запушені:

```bash
git add render.yaml
git commit -m "Add automatic migrations on start"
git push
```

Render автоматично визначить зміни та перезапустить сервіс.

## Перевірка після перезапуску

1. Перевірте логи на Render - має з'явитися:
   ```
   🔄 Running database migrations...
   ✅ Migrations completed successfully
   🚀 Server running on http://localhost:10000
   ```

2. Спробуйте створити акаунт знову - має працювати

3. Перевірте health check:
   ```
   https://booking-system-api-ipjr.onrender.com/health
   ```

## Якщо все ще не працює

Перевірте логи на Render на наявність помилок:
- Якщо бачите помилку про підключення до бази даних - перевірте `DATABASE_URL`
- Якщо бачите помилку про відсутність Prisma - перевірте, чи виконався `prisma:generate` в buildCommand

