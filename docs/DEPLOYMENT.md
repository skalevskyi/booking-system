# 🚀 Deployment Guide

Цей гайд описує процес деплою frontend на GitHub Pages та backend на Render/Railway.

---

## Frontend: GitHub Pages

### Передумови

1. Репозиторій має бути публічним (або GitHub Pro для приватних)
2. GitHub Actions має бути увімкнено для репозиторію

### Крок 1: Налаштування GitHub Pages

1. Перейдіть до **Settings** → **Pages** у вашому репозиторії
2. У розділі **Source** виберіть:
   - **Source**: `GitHub Actions`
3. Збережіть зміни

### Крок 2: Налаштування API URL

1. Перейдіть до **Settings** → **Secrets and variables** → **Actions**
2. Додайте новий secret:
   - **Name**: `VITE_API_URL`
   - **Value**: URL вашого backend API (наприклад, `https://your-api.onrender.com`)

### Крок 3: Деплой

Після push до гілки `main` (з змінами в `apps/web/**`), GitHub Actions автоматично:
1. Збудує frontend з правильним `base: '/booking-system/'`
2. Створить SPA fallback (`404.html`)
3. Задеплоїть на GitHub Pages

**URL вашого сайту буде:**
```
https://YOUR_USERNAME.github.io/booking-system/
```

**Workflow файл:** `.github/workflows/deploy-web.yml`

**Важливо:** Переконайтеся, що `base: '/booking-system/'` встановлено в `apps/web/vite.config.ts`

---

## Backend: Render

### Крок 1: Створення облікового запису

1. Перейдіть на https://render.com
2. Зареєструйтеся через GitHub

### Крок 2: Створення PostgreSQL Database

1. Натисніть **New +** → **PostgreSQL**
2. Заповніть форму:
   - **Name**: `booking-system-db`
   - **Database**: `booking`
   - **User**: `postgres`
   - **Region**: найближчий до вас
3. Натисніть **Create Database**
4. Скопіюйте **Internal Database URL** (потрібен буде пізніше)

### Крок 3: Створення Web Service

**Варіант A: Автоматичний деплой через render.yaml (рекомендовано)**

1. Натисніть **New +** → **Blueprint**
2. Підключіть ваш GitHub репозиторій
3. Render автоматично визначить `render.yaml` і створить сервіс з усіма налаштуваннями
4. Перевірте, що в налаштуваннях сервісу:
   - ✅ **Auto-Deploy**: `On commit` (автоматичний деплой при push до main)
   - ✅ **Health Check Path**: `/health`
   - ✅ **Root Directory**: `apps/api`

**Варіант B: Ручне створення**

1. Натисніть **New +** → **Web Service**
2. Підключіть ваш GitHub репозиторій
3. Заповніть форму:
   - **Name**: `booking-system-api`
   - **Region**: той самий, що й база даних
   - **Branch**: `main`
   - **Root Directory**: `apps/api`
   - **Environment**: `Node`
   - **Build Command**: `corepack enable && corepack prepare pnpm@latest --activate && pnpm install --frozen-lockfile && pnpm approve-builds bcrypt @prisma/client prisma && pnpm prisma:generate && pnpm prisma:migrate:deploy && pnpm build`
   - **Start Command**: `pnpm prisma migrate deploy && (pnpm prisma:seed || echo "Seed skipped or already done") && pnpm start`
4. У налаштуваннях сервісу:
   - ✅ **Auto-Deploy**: `On commit` (виберіть з dropdown)
   - ✅ **Health Check Path**: `/health`

### Крок 4: Налаштування Environment Variables

У розділі **Environment** додайте:

```
NODE_ENV=production
PORT=10000
DATABASE_URL=<Internal Database URL з кроку 2>
JWT_SECRET=<ваш секретний ключ мінімум 32 символи>
JWT_REFRESH_SECRET=<ваш секретний ключ мінімум 32 символи>
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
```

**Важливо:** 
- `JWT_SECRET` та `JWT_REFRESH_SECRET` мають бути різними та безпечними
- Використовуйте генератор: `openssl rand -base64 32`

### Крок 5: Виконання міграцій

**Важливо:** Міграції тепер автоматично виконуються під час build процесу (включено в `render.yaml`).

Якщо після першого деплою таблиці не створені, виконайте міграції вручну:

1. Перейдіть до **Shell** у вашому Web Service
2. Виконайте:
```bash
cd apps/api
pnpm prisma migrate deploy
```

**Примітка:** Seed дані (якщо потрібні) можна додати після міграцій:
```bash
pnpm tsx prisma/seed.ts
```

### Крок 6: Отримання URL

Після деплою Render надасть URL типу:
```
https://booking-system-api.onrender.com
```

**Оновіть `VITE_API_URL` secret у GitHub** з цим URL.

---

## Backend: Railway

### Крок 1: Створення облікового запису

1. Перейдіть на https://railway.app
2. Зареєструйтеся через GitHub

### Крок 2: Створення проекту

1. Натисніть **New Project**
2. Виберіть **Deploy from GitHub repo**
3. Виберіть ваш репозиторій

### Крок 3: Додавання PostgreSQL

1. Натисніть **+ New** → **Database** → **PostgreSQL**
2. Railway автоматично створить базу даних
3. Скопіюйте **DATABASE_URL** з вкладки **Variables**

### Крок 4: Налаштування сервісу

1. Натисніть **+ New** → **GitHub Repo**
2. Виберіть ваш репозиторій
3. Railway автоматично визначить Node.js проект

### Крок 5: Налаштування змінних оточення

У розділі **Variables** додайте:

```
NODE_ENV=production
PORT=${{PORT}}
DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET=<ваш секретний ключ мінімум 32 символи>
JWT_REFRESH_SECRET=<ваш секретний ключ мінімум 32 символи>
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
```

### Крок 6: Налаштування Root Directory

У **Settings** → **Root Directory** вкажіть:
```
apps/api
```

### Крок 7: Налаштування Build та Start команд

У **Settings** → **Deploy**:
- **Build Command**: `pnpm install && pnpm prisma:generate && pnpm build`
- **Start Command**: `pnpm start`

### Крок 8: Виконання міграцій

1. Відкрийте **Terminal** у вашому сервісі
2. Виконайте:
```bash
cd apps/api
pnpm prisma:migrate deploy
pnpm tsx prisma/seed.ts
```

### Крок 9: Отримання URL

Railway автоматично надасть URL типу:
```
https://booking-system-production.up.railway.app
```

**Оновіть `VITE_API_URL` secret у GitHub** з цим URL.

---

## Оновлення Frontend для Production

### Автоматичне (через GitHub Secrets)

Якщо ви налаштували `VITE_API_URL` secret у GitHub, frontend автоматично використає його під час збірки.

### Ручне оновлення

Якщо потрібно змінити URL вручну, оновіть `.github/workflows/deploy-web.yml`:

```yaml
env:
  VITE_API_URL: 'https://your-api-url.onrender.com'
```

**Або** додайте/оновіть secret `VITE_API_URL` у GitHub Settings → Secrets and variables → Actions

---

## Перевірка деплою

### Frontend

1. Відкрийте `https://YOUR_USERNAME.github.io/booking-system/`
2. Перевірте, що сторінка завантажується
3. Перевірте консоль браузера на помилки

### Backend

1. Відкрийте `https://your-api-url.onrender.com/health`
2. Має повернутися: `{"status":"ok"}`
3. Відкрийте `https://your-api-url.onrender.com/docs`
4. Має відкритися Swagger UI

---

## Troubleshooting

### Frontend не підключається до API

1. Перевірте, що `VITE_API_URL` правильно налаштований
2. Перевірте CORS налаштування на backend
3. Перевірте консоль браузера на помилки

### Backend не підключається до бази даних

1. Перевірте, що `DATABASE_URL` правильний
2. Перевірте, що база даних запущена
3. Перевірте логи на Render/Railway

### Міграції не виконуються

1. Переконайтеся, що Prisma Client згенеровано
2. Виконайте міграції вручну через Shell/Terminal
3. Перевірте, що `DATABASE_URL` правильний

---

## CORS налаштування

Переконайтеся, що backend дозволяє запити з вашого frontend домену. У `apps/api/src/index.ts` перевірте CORS налаштування:

```typescript
await fastify.register(cors, {
  origin: [
    'http://localhost:5173',
    'https://YOUR_USERNAME.github.io',
  ],
  credentials: true,
});
```

---

## Оновлення після змін

### Frontend

Просто зробіть push до `main` гілки - GitHub Actions автоматично задеплоїть зміни.

### Backend

**Render:**
- Автоматичний деплой налаштований через `render.yaml` з `autoDeploy: true`
- Після push до `main` гілки Render автоматично визначить зміни та задеплоїть
- Health check endpoint (`/health`) допомагає підтримувати сервіс активним
- Keep-alive workflow (`.github/workflows/keep-alive.yml`) пінг сервіс кожні 10 хвилин

**Railway:**
- Автоматично перезапустить сервіс після push до `main` гілки

---

**Готово!** 🎉 Ваш проект тепер задеплоєний.

