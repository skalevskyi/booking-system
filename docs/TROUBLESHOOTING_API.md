# Troubleshooting: API не працює

Якщо API endpoints не працюють (`http://localhost:4000`, `/docs`, `/health`), виконайте наступні кроки:

## Крок 1: Перевірте, чи запущений API сервер

### Перевірка в терміналі

Якщо ви запускали `pnpm dev`, перевірте термінал. Має бути повідомлення:
```
🚀 Server running on http://localhost:4000
```

Якщо цього повідомлення немає, API сервер не запущений.

### Запуск API окремо для діагностики

Відкрийте новий термінал і виконайте:

```powershell
cd apps/api
pnpm dev
```

Це покаже всі помилки, якщо вони є.

## Крок 2: Перевірте .env файл

Переконайтеся, що файл `apps/api/.env` існує і містить:

```env
NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/booking?schema=public
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-long-change-in-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
```

**Якщо файл не існує**, створіть його:

```powershell
pnpm setup:env
```

Або вручну створіть файл `apps/api/.env` з вмістом вище.

## Крок 3: Перевірте базу даних

API сервер потребує підключення до PostgreSQL. Перевірте:

```powershell
# Перевірте, чи запущений Docker контейнер
docker ps

# Якщо контейнер не запущений:
docker compose up -d

# Перевірте логи бази даних:
docker compose logs db
```

## Крок 4: Типові помилки та рішення

### Помилка: "Invalid environment variables"

**Причина:** Відсутні або неправильні змінні в `.env` файлі.

**Рішення:**
1. Перевірте, що всі змінні присутні в `apps/api/.env`
2. Переконайтеся, що `JWT_SECRET` та `JWT_REFRESH_SECRET` мають мінімум 32 символи
3. Переконайтеся, що `DATABASE_URL` правильний

### Помилка: "Cannot connect to database"

**Причина:** PostgreSQL не запущений або неправильний `DATABASE_URL`.

**Рішення:**
1. Запустіть Docker контейнер: `docker compose up -d`
2. Перевірте, що контейнер працює: `docker ps`
3. Перевірте `DATABASE_URL` у `.env` файлі

### Помилка: "Port 4000 is already in use"

**Причина:** Порт 4000 зайнятий іншим процесом.

**Рішення:**
1. Знайдіть процес, який використовує порт:
   ```powershell
   netstat -ano | findstr :4000
   ```
2. Зупиніть процес або змініть PORT у `.env` файлі

### Помилка: "Prisma Client not generated"

**Причина:** Prisma Client не згенеровано.

**Рішення:**
```powershell
cd apps/api
pnpm prisma:generate
```

### Помилка: "Migration not applied"

**Причина:** Міграції не виконані.

**Рішення:**
```powershell
cd apps/api
pnpm prisma:migrate
```

## Крок 5: Проблеми з нативними модулями

### bcrypt - Cannot find module 'bcrypt_lib.node'

**Проблема:** pnpm ігнорує build scripts для bcrypt, тому нативний модуль не компілюється.

**Рішення:**
```powershell
cd apps/api
pnpm approve-builds bcrypt
pnpm rebuild bcrypt
```

**Альтернатива:** Якщо не працює, можна використати `bcryptjs` (JavaScript реалізація):
```powershell
cd apps/api
pnpm remove bcrypt @types/bcrypt
pnpm add bcryptjs
pnpm add -D @types/bcryptjs
```
Потрібно оновити імпорт в `apps/api/src/lib/auth.ts`: `import bcrypt from 'bcryptjs'`

### rollup - Cannot find module '@rollup/rollup-win32-x64-msvc'

**Проблема:** Відсутній optional dependency для rollup.

**Рішення:**
```powershell
cd apps/web
pnpm approve-builds rollup
pnpm install --force
```

### Prisma Client - Cannot find module 'PrismaClient'

**Проблема:** Prisma Client не згенеровано після перевстановлення залежностей.

**Рішення:**
```powershell
cd apps/api
pnpm approve-builds @prisma/client prisma
pnpm prisma:generate
```

## Крок 6: Проблеми сумісності версій

### Swagger - expected '5.x' fastify version

Якщо бачите помилку `expected '5.x' fastify version, '4.29.1' is installed`:

**Рішення:** Використовуються правильні сумісні версії:
- `@fastify/swagger@^8.15.0` (для Fastify 4.x)
- `@fastify/swagger-ui@^2.1.0` (для Fastify 4.x)

Якщо помилка залишається:
```powershell
cd apps/api
pnpm install @fastify/swagger@^8.15.0 @fastify/swagger-ui@^2.1.0
```

## Крок 7: Повний перезапуск

Якщо нічого не допомагає, виконайте повний перезапуск:

```powershell
# 1. Зупиніть всі процеси (Ctrl+C у всіх терміналах)

# 2. Перевірте та створіть .env файли
pnpm setup:env

# 3. Запустіть базу даних
docker compose up -d

# 4. Дозволити build scripts
cd apps/api
pnpm approve-builds bcrypt @prisma/client prisma
cd ../web
pnpm approve-builds rollup
cd ../api

# 5. Згенеруйте Prisma Client
pnpm prisma:generate
cd ../..

# 6. Запустіть проект
pnpm dev
```

## Крок 8: Перевірка після запуску

Після запуску перевірте:

1. **Health check:**
   ```
   http://localhost:4000/health
   ```
   Має повернути: `{"status":"ok"}`

2. **Swagger UI:**
   ```
   http://localhost:4000/docs
   ```
   Має відкритися Swagger документація

3. **API Base:**
   ```
   http://localhost:4000
   ```
   Може повернути 404, це нормально

## Додаткові проблеми

### Помилка імпорту "bookingRoutes" або "serviceRoutes"

Якщо бачите помилки типу `does not provide an export named 'bookingRoutes'`:

**Рішення:** Це вже виправлено в коді. Перезапустіть сервер:
```powershell
# Зупиніть сервер (Ctrl+C)
# Запустіть знову
pnpm dev
```

### Помилка "fastify is not defined" в error handler

Якщо бачите помилку `ReferenceError: fastify is not defined`:

**Рішення:** Це вже виправлено в коді. Перезапустіть сервер.

### Помилка "ERR_CONNECTION_REFUSED"

Якщо frontend не може підключитися до API:

1. Перевірте, що API сервер запущений (`🚀 Server running on http://localhost:4000`)
2. Перевірте, що обидва сервери запущені одночасно
3. Frontend використовує Vite proxy - переконайтеся, що обидва сервери працюють

## Якщо проблема залишається

1. Перевірте логи в терміналі, де запущений API сервер
2. Перевірте, чи немає помилок компіляції TypeScript
3. Перевірте, чи всі залежності встановлені: `cd apps/api && pnpm install`
4. Перевірте версію Node.js (потрібна 20+): `node --version`
5. Дивіться детальні інструкції у **QUICK_START.md** (розділ Troubleshooting)

## Додаткова діагностика

### Перевірка, чи працює API сервер

```powershell
# Спробуйте підключитися через curl або PowerShell
Invoke-WebRequest -Uri http://localhost:4000/health -Method GET
```

Якщо отримуєте помилку підключення, API сервер точно не працює.

### Перевірка процесів Node.js

```powershell
Get-Process | Where-Object {$_.ProcessName -like "*node*"}
```

Якщо немає процесів Node.js, сервер не запущений.

## Діагностика помилки 500 на Production (Render)

Якщо на production сервері (Render) виникає помилка 500 при реєстрації або логіні:

### Крок 1: Перевірте логи на Render

1. Перейдіть до вашого Render dashboard
2. Виберіть ваш API сервіс
3. Відкрийте вкладку **Logs**
4. Знайдіть помилки, пов'язані з `/api/auth/register` або `/api/auth/login`

### Крок 2: Перевірте змінні середовища на Render

Переконайтеся, що всі необхідні змінні налаштовані:

1. Перейдіть до **Environment** у налаштуваннях сервісу
2. Перевірте наявність:
   - `DATABASE_URL` - URL до PostgreSQL бази даних
   - `JWT_SECRET` - мінімум 32 символи
   - `JWT_REFRESH_SECRET` - мінімум 32 символи
   - `JWT_ACCESS_EXPIRES_IN` - наприклад, `15m`
   - `JWT_REFRESH_EXPIRES_IN` - наприклад, `7d`
   - `NODE_ENV` - має бути `production`
   - `PORT` - Render автоматично встановлює, але можна вказати явно

### Крок 3: Типові причини помилки 500

#### Проблема: JWT secrets не налаштовані

**Симптоми:**
- Помилка 500 при реєстрації/логіні
- У логах: "JWT secrets are not configured" або "secretOrPrivateKey must have a value"

**Рішення:**
1. Перевірте, що `JWT_SECRET` та `JWT_REFRESH_SECRET` встановлені в Environment variables
2. Переконайтеся, що вони мають мінімум 32 символи
3. Перезапустіть сервіс після зміни змінних

#### Проблема: База даних не підключена

**Симптоми:**
- Помилка 500 з повідомленням про підключення до бази даних
- У логах: "Can't reach database server" або "Connection refused"

**Рішення:**
1. Перевірте, що PostgreSQL база даних створена на Render
2. Перевірте, що `DATABASE_URL` правильний (включає пароль, хост, порт)
3. Переконайтеся, що база даних активна (не в сплячому режимі)

#### Проблема: Міграції не виконані

**Симптоми:**
- Помилка 500 з повідомленням про відсутні таблиці
- У логах: "relation does not exist", "table does not exist" або "The table `public.users` does not exist"

**Рішення:**

**Автоматичне виправлення:**
Міграції тепер автоматично виконуються при кожному старті сервера (додано в `startCommand` у `render.yaml`). 

**Що робити зараз:**
1. Зробіть commit та push змін до репозиторію
2. Render автоматично перезапустить сервіс з новим `startCommand`
3. Під час перезапуску міграції виконаються автоматично
4. Після успішного перезапуску спробуйте створити акаунт знову

**Альтернативний варіант (якщо Shell доступний):**
Якщо вкладка Shell працює, можна виконати міграції вручну:
1. Перейдіть до Render dashboard → ваш API сервіс
2. Відкрийте вкладку **Shell**
3. Виконайте:
   ```bash
   cd apps/api
   pnpm prisma migrate deploy
   ```

#### Проблема: Prisma Client не згенеровано

**Симптоми:**
- Помилка 500 з повідомленням про Prisma Client
- У логах: "Cannot find module '@prisma/client'"

**Рішення:**
1. Переконайтеся, що в build команді генерується Prisma Client:
   ```bash
   cd apps/api && pnpm prisma generate && pnpm build && pnpm start
   ```

### Крок 4: Детальне логування помилок

Після оновлення коду, error handler тепер логує детальну інформацію:

1. Перевірте логи на Render
2. Знайдіть записи з тегом "Request error" або "Registration error"
3. Перевірте:
   - URL запиту
   - Метод (POST, GET, тощо)
   - Тіло запиту (без паролів)
   - Статус код помилки
   - Повідомлення про помилку

### Крок 5: Тестування API напряму

Використайте curl або Postman для тестування:

```bash
# Тест реєстрації
curl -X POST https://your-api.onrender.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "name": "Test User",
    "password": "password123"
  }'

# Тест health check
curl https://your-api.onrender.com/health
```

### Крок 6: Перевірка CORS налаштувань

Якщо помилка виникає тільки з frontend:

1. Перевірте, що `FRONTEND_URL` встановлено правильно
2. Переконайтеся, що в CORS налаштуваннях є ваш GitHub Pages URL:
   - `https://skalevskyi.github.io`
   - `https://skalevskyi.github.io/booking-system`

### Крок 7: Перезапуск сервісу

Після зміни змінних середовища або конфігурації:

1. Перейдіть до **Manual Deploy** → **Deploy latest commit**
2. Або просто зробіть новий commit і push - Render автоматично перезапустить

### Якщо проблема залишається

1. Перевірте повні логи на Render (можуть бути приховані помилки)
2. Перевірте, чи всі залежності правильно встановлені
3. Перевірте версію Node.js (Render має використовувати Node 20+)
4. Спробуйте запустити API локально з production змінними для тестування

