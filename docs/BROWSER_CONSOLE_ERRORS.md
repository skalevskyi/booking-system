# 🔍 Пояснення помилок у консолі браузера

Цей документ пояснює, які помилки в консолі браузера можна ігнорувати, а які потребують уваги.

## ✅ Помилки, які можна ігнорувати

### 1. Помилки від Chrome Extensions (AdBlock, тощо)

```
Error handling response: TypeError: Cannot read properties of undefined (reading 'indexOf')
    at chrome-extension://imgpenhngnbnmhdkpdfnfhdpmfgmihdn/adblock/content/counter.js:17:15
```

**Пояснення:** Це помилки від розширень браузера (наприклад, AdBlock), які намагаються обробити сторінку. Вони не впливають на роботу вашого додатку.

**Рішення:** Можна ігнорувати або вимкнути розширення для тестування.

### 2. 404 помилки для SPA роутів (якщо SPA працює)

```
GET https://skalevskyi.github.io/booking-system/bookings/new 404 (Not Found)
Failed to load resource: the server responded with a status of 404
```

**Пояснення:** GitHub Pages спочатку намагається знайти фізичний файл `/bookings/new`, але його немає (це SPA). Потім GitHub Pages показує `404.html`, який містить ваш React додаток, і React Router обробляє роут.

**Як перевірити:**
- Якщо сторінка все одно відкривається і працює - все добре ✅
- Якщо сторінка не відкривається - потрібно перевірити налаштування

**Рішення:** 
- Переконайтеся, що у workflow є крок "SPA fallback" (копіювання `index.html` в `404.html`)
- Переконайтеся, що `base: '/booking-system/'` встановлено в `vite.config.ts`
- Переконайтеся, що `basename="/booking-system"` встановлено в `BrowserRouter`

### 3. Помилки від інших розширень

```
Ex.js:1269 version: 14
Ex.js:3751 before all_funcdisable
FingerPrint.js:19 has get the FIngerPrintSwitch
```

**Пояснення:** Це помилки від інших розширень браузера (можливо, privacy extensions, fingerprinting protection, тощо).

**Рішення:** Можна ігнорувати.

## ⚠️ Помилки, які потребують уваги

### 1. CORS помилки

```
Access to XMLHttpRequest at 'https://api.example.com/api/services' from origin 'https://skalevskyi.github.io' 
has been blocked by CORS policy
```

**Пояснення:** Backend не дозволяє запити з вашого frontend домену.

**Рішення:** Перевірте CORS налаштування на backend.

### 2. Помилки підключення до API

```
GET https://booking-system-api-ipjr.onrender.com/api/services net::ERR_FAILED
POST https://booking-system-api-ipjr.onrender.com/api/auth/register 500 (Internal Server Error)
```

**Пояснення:** Проблеми з підключенням до API або помилки на сервері.

**Рішення:** 
- Перевірте, чи API сервер працює
- Перевірте логи на Render
- Перевірте змінні середовища

### 3. Помилки завантаження ресурсів (JS/CSS)

```
Failed to load resource: the server responded with a status of 404
index-DDiN6u9W.js:1 Failed to load resource
```

**Пояснення:** JavaScript або CSS файли не завантажуються.

**Рішення:**
- Перевірте, чи правильно налаштований `base` path в `vite.config.ts`
- Перевірте, чи файли існують в `dist` після build
- Очистіть кеш браузера

### 4. Помилки React Router

```
Uncaught Error: No routes matched location "/bookings/new"
```

**Пояснення:** React Router не може знайти роут.

**Рішення:**
- Перевірте, чи правильно налаштований `basename` в `BrowserRouter`
- Перевірте, чи роут існує в `App.tsx`

## 📋 Чеклист для перевірки

Якщо бачите помилки в консолі:

1. ✅ **Перевірте, чи додаток працює функціонально**
   - Чи відкриваються сторінки?
   - Чи працюють форми?
   - Чи завантажуються дані з API?

2. ✅ **Перевірте тип помилки**
   - Якщо це помилки від розширень - ігноруйте
   - Якщо це CORS/API помилки - виправте

3. ✅ **Перевірте в режимі інкогніто**
   - Відкрийте сайт в режимі інкогніто (без розширень)
   - Якщо помилки зникли - це були помилки від розширень

4. ✅ **Перевірте в різних браузерах**
   - Спробуйте Chrome, Firefox, Safari
   - Якщо помилки тільки в одному браузері - можливо, це розширення

## 🎯 Висновок

**Якщо додаток працює функціонально**, більшість помилок в консолі можна ігнорувати, особливо:
- Помилки від розширень браузера
- 404 помилки для SPA роутів (якщо сторінки все одно працюють)
- Попередження про неіснуючі ресурси (якщо вони не впливають на функціональність)

**Звертайте увагу на:**
- CORS помилки
- Помилки підключення до API
- Помилки завантаження основних ресурсів (JS/CSS)
- Помилки React Router

