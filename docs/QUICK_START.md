# Швидкий запуск проекту для перегляду та валідації

## ⚡ Найшвидший спосіб (3 команди)

```powershell
# 1. Створити .env файли
pnpm setup:env

# 2. Запустити базу даних
pnpm docker:up

# 3. Запустити проект
pnpm dev
```

Після цього відкрийте:
- **Frontend**: http://localhost:5173
- **API Docs**: http://localhost:4000/docs

**⚠️ Важливо:** Після змін у коді або конфігурації перезапустіть dev сервери (Ctrl+C, потім `pnpm dev` знову), щоб зміни застосувалися.

---

## Детальні інструкції

### Крок 1: Створення файлів конфігурації

### Автоматичне створення (рекомендовано)

Використайте вбудований скрипт для автоматичного створення `.env` файлів:

```powershell
pnpm setup:env
```

Це створить обидва `.env` файли з правильними налаштуваннями.

### Ручне створення (альтернатива)

Якщо потрібно створити файли вручну:

#### API (`apps/api/.env`)

```env
NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/booking?schema=public
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-long-change-in-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
```

#### Web (`apps/web/.env`)

```env
VITE_API_URL=http://localhost:4000
```

**⚠️ ВАЖЛИВО:** Після створення файлів змініть `JWT_SECRET` та `JWT_REFRESH_SECRET` у `apps/api/.env` на безпечні випадкові значення (мінімум 32 символи).

## Крок 2: Перевірка бази даних

Переконайтеся, що PostgreSQL контейнер запущений:

```powershell
docker ps
```

Якщо контейнер не запущений, виконайте:

```powershell
docker compose up -d
```

## Крок 3: Встановлення залежностей (якщо ще не встановлено)

```powershell
# Кореневі залежності
pnpm install

# Залежності API
cd apps/api
pnpm install
cd ../..

# Залежності Web
cd apps/web
pnpm install
cd ../..
```

**Примітка:** Після встановлення залежностей може знадобитися:
- Дозволити build scripts для нативних модулів: `pnpm approve-builds bcrypt` (в apps/api)
- Згенерувати Prisma Client: `cd apps/api && pnpm prisma:generate`

## Крок 4: Налаштування нативних модулів (якщо потрібно)

Після першого встановлення залежностей може знадобитися дозволити build scripts:

```powershell
# Для API (bcrypt)
cd apps/api
pnpm approve-builds bcrypt

# Для Web (rollup) - зазвичай не потрібно, але якщо є помилки:
cd ../web
pnpm approve-builds rollup

# Для Prisma (якщо потрібно)
cd ../api
pnpm approve-builds @prisma/client prisma
```

## Крок 5: Запуск проекту

### Варіант A: Запуск обох серверів одночасно (рекомендовано)

З кореневої папки проекту:

```powershell
pnpm dev
```

Це запустить:
- **API сервер** на `http://localhost:4000`
- **Web додаток** на `http://localhost:5173`

**⚠️ ВАЖЛИВО:** Переконайтеся, що обидва сервери запустилися успішно. У терміналі має бути:
- `🚀 Server running on http://localhost:4000` (для API)
- `Local: http://localhost:5173/` (для Web)

Якщо один з серверів не запустився, перевірте помилки в терміналі та виправте їх перед продовженням. Дивіться розділ **Troubleshooting** нижче.

### Варіант B: Запуск окремо

**Термінал 1 - API:**
```powershell
cd apps/api
pnpm dev
```

**Термінал 2 - Web:**
```powershell
cd apps/web
pnpm dev
```

## Крок 6: Перегляд та валідація

### 1. Відкрийте веб-додаток

Перейдіть у браузері за адресою:
```
http://localhost:5173
```

### 2. Створіть акаунт або увійдіть

- Натисніть "Register" для створення нового акаунта
- Або "Login" якщо вже маєте акаунт

### 3. Перевірте функціональність

- **Dashboard**: Перегляд послуг та бронювань
- **New Booking**: Створення нового бронювання
- **Services**: Перегляд доступних послуг

### 4. API Документація

Відкрийте Swagger UI документацію:
```
http://localhost:4000/docs
```

Тут ви можете:
- Переглянути всі доступні endpoints
- Протестувати API напряму через інтерфейс
- Перевірити схеми запитів та відповідей

### 5. Health Check

Перевірте, що API працює:
```
http://localhost:4000/health
```

Повинна повернутися відповідь: `{"status":"ok"}`

## Корисні команди для валідації

### Перевірка бази даних через Prisma Studio

```powershell
cd apps/api
pnpm prisma:studio
```

Відкриється візуальний редактор БД на `http://localhost:5555`

### Перевірка логів Docker контейнера

```powershell
docker compose logs -f db
```

### Запуск тестів API

```powershell
cd apps/api
pnpm test
```

### Перевірка статусу контейнерів

```powershell
docker ps
```

## Troubleshooting

### Помилка підключення до бази даних

1. Перевірте, що Docker Desktop запущений
2. Перевірте статус контейнера: `docker ps`
3. Перевірте логи: `docker compose logs db`
4. Переконайтеся, що `DATABASE_URL` у `apps/api/.env` правильний

### Помилка "Port already in use"

- **Порт 4000 (API)**: Змініть `PORT` у `apps/api/.env`
- **Порт 5173 (Web)**: Vite автоматично запропонує інший порт

### Помилка міграцій Prisma

```powershell
cd apps/api
pnpm prisma:generate
pnpm prisma:migrate
```

### Помилка "Cannot find module"

Перевстановіть залежності:

```powershell
cd apps/api
pnpm install
cd ../web
pnpm install
```

### Помилка "ERR_CONNECTION_REFUSED" при реєстрації/вході

Ця помилка означає, що API сервер не запущений або недоступний.

**Рішення:**

1. **Перевірте, чи запущений API сервер:**
   ```powershell
   # Відкрийте http://localhost:4000/health у браузері
   # Або перевірте в терміналі, чи є процес на порту 4000
   ```

2. **Перезапустіть обидва сервери:**
   ```powershell
   # Зупиніть поточні процеси (Ctrl+C)
   # Потім запустіть знову:
   pnpm dev
   ```

3. **Перевірте, чи немає помилок у терміналі:**
   - API сервер має показувати: `🚀 Server running on http://localhost:4000`
   - Web сервер має показувати: `Local: http://localhost:5173/`

4. **Якщо API не запускається, перевірте помилки:**
   ```powershell
   cd apps/api
   pnpm dev
   ```

**Примітка:** Frontend використовує Vite proxy для автоматичного перенаправлення запитів `/api/*` на `http://localhost:4000`. Переконайтеся, що обидва сервери запущені одночасно.

### Помилка імпорту "bookingRoutes" або "serviceRoutes"

Якщо бачите помилки типу `The requested module './routes/bookings.js' does not provide an export named 'bookingRoutes'`:

**Рішення:** Це вже виправлено в коді. Якщо помилка залишається:
1. Перезапустіть API сервер
2. Перевірте, що використовуються правильні імпорти: `bookingsRoutes` та `servicesRoutes` (з 's')

### Помилка з нативними модулями (bcrypt, rollup)

Якщо бачите помилки типу `Cannot find module 'bcrypt_lib.node'` або `Cannot find module '@rollup/rollup-win32-x64-msvc'`:

**Рішення:**

1. **Дозволити build scripts:**
   ```powershell
   cd apps/api
   pnpm approve-builds bcrypt
   cd ../web
   pnpm approve-builds rollup
   ```

2. **Перебудувати нативні модулі:**
   ```powershell
   cd apps/api
   pnpm rebuild bcrypt
   ```

3. **Перевстановити залежності:**
   ```powershell
   cd apps/api
   pnpm install --force
   cd ../web
   pnpm install --force
   ```

### Помилка сумісності версій Swagger

Якщо бачите помилку `expected '5.x' fastify version, '4.29.1' is installed`:

**Рішення:** Використовуються правильні сумісні версії:
- `@fastify/swagger@^8.15.0` (для Fastify 4.x)
- `@fastify/swagger-ui@^2.1.0` (для Fastify 4.x)

Якщо помилка залишається:
```powershell
cd apps/api
pnpm install @fastify/swagger@^8.15.0 @fastify/swagger-ui@^2.1.0
```

### Помилка "PrismaClient is not defined"

Якщо бачите помилку `The requested module '@prisma/client' does not provide an export named 'PrismaClient'`:

**Рішення:**

1. **Дозволити build scripts для Prisma:**
   ```powershell
   cd apps/api
   pnpm approve-builds @prisma/client prisma
   ```

2. **Згенерувати Prisma Client:**
   ```powershell
   pnpm prisma:generate
   ```

### Помилка "fastify is not defined" в error handler

Якщо бачите помилку `ReferenceError: fastify is not defined`:

**Рішення:** Це вже виправлено в коді. Якщо помилка залишається, перезапустіть сервер.

## Структура URL-адрес для тестування

- **Frontend**: http://localhost:5173
- **API Base**: http://localhost:4000
- **API Docs**: http://localhost:4000/docs
- **Health Check**: http://localhost:4000/health
- **Prisma Studio**: http://localhost:5555 (після запуску `pnpm prisma:studio`)

## Основні endpoints для тестування

### Authentication
- `POST /api/auth/register` - Реєстрація
- `POST /api/auth/login` - Вхід
- `GET /api/auth/me` - Поточний користувач (потрібна авторизація)

### Services
- `GET /api/services` - Список послуг (публічний)
- `GET /api/services/:id` - Деталі послуги (публічний)

### Bookings
- `POST /api/bookings` - Створення бронювання (потрібна авторизація)
- `GET /api/bookings` - Список бронювань (потрібна авторизація)

Всі endpoints можна протестувати через Swagger UI на `/docs`

