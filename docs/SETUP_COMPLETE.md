# ✅ Повний чеклист налаштування проекту

Виконайте ці кроки по порядку для повного налаштування проекту.

## ✅ Крок 1: Перевірка вимог

- [ ] Node.js 20+ встановлено (`node --version`)
- [ ] pnpm встановлено (`pnpm --version`)
- [ ] Docker Desktop встановлено та запущений (`docker --version`)

## ✅ Крок 2: Створення .env файлів

```powershell
pnpm setup:env
```

Або створіть вручну:
- `apps/api/.env` - дивіться QUICK_START.md
- `apps/web/.env` - дивіться QUICK_START.md

## ✅ Крок 3: Запуск бази даних

```powershell
docker compose up -d
```

Перевірка:
```powershell
docker ps
```

## ✅ Крок 4: Встановлення залежностей

```powershell
pnpm install
```

## ✅ Крок 5: Налаштування нативних модулів

```powershell
# API - bcrypt
cd apps/api
pnpm approve-builds bcrypt

# Prisma
pnpm approve-builds @prisma/client prisma

# Web - rollup (якщо потрібно)
cd ../web
pnpm approve-builds rollup
```

## ✅ Крок 6: Генерація Prisma Client

```powershell
cd apps/api
pnpm prisma:generate
```

## ✅ Крок 7: Виконання міграцій

```powershell
pnpm prisma:migrate
```

## ✅ Крок 8: Запуск проекту

```powershell
cd ../..
pnpm dev
```

## ✅ Крок 9: Перевірка роботи

Відкрийте в браузері:
- [ ] `http://localhost:4000/health` - має повернути `{"status":"ok"}`
- [ ] `http://localhost:4000/docs` - має відкритися Swagger UI
- [ ] `http://localhost:5173` - має відкритися сторінка входу/реєстрації

## ✅ Перевірка в терміналі

Має бути:
- `🚀 Server running on http://localhost:4000` (API)
- `Local: http://localhost:5173/` (Web)

## Якщо щось не працює

Дивіться детальні інструкції у:
- **QUICK_START.md** - розділ Troubleshooting
- **TROUBLESHOOTING_API.md** - діагностика API
- **FIX_NATIVE_MODULES.md** - виправлення нативних модулів

## Після першого налаштування

Надалі для запуску достатньо:

```powershell
# 1. Запустити базу даних (якщо не запущена)
docker compose up -d

# 2. Запустити проект
pnpm dev
```

Кроки 1-7 потрібні лише один раз (або після змін у конфігурації/схемі БД).

