# 🐳 Вирішення проблем з Docker

## Проблема: Docker Desktop не запущений

### Помилка:
```
error during connect: Get "http://%2F%2F.%2Fpipe%2FdockerDesktopLinuxEngine/v1.51/...": 
open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.
```

### Рішення:

#### Крок 1: Запустити Docker Desktop

1. **Знайдіть Docker Desktop** на вашому комп'ютері:
   - У меню "Пуск" знайдіть "Docker Desktop"
   - Або знайдіть іконку Docker у системному треї (правому нижньому куті)

2. **Запустіть Docker Desktop:**
   - Подвійний клік на іконку Docker Desktop
   - Або натисніть праву кнопку миші на іконці в треї → "Open Docker Desktop"

3. **Зачекайте, поки Docker запуститься:**
   - Іконка Docker в треї перестане анімуватися
   - Відкриється вікно Docker Desktop
   - Статус зміниться на "Docker Desktop is running"

#### Крок 2: Перевірити, що Docker працює

Відкрийте PowerShell та виконайте:

```powershell
# Перевірка версії Docker
docker --version

# Перевірка, що Docker працює
docker ps

# Якщо команда `docker ps` працює без помилок - Docker запущений!
```

#### Крок 3: Запустити базу даних

Після того, як Docker Desktop запущений:

```powershell
# Запустити PostgreSQL контейнер
pnpm docker:up

# Перевірити, що контейнер працює
docker ps
```

Ви повинні побачити контейнер `booking_system-postgres-1` в списку.

#### Крок 4: Запустити проект

```powershell
# Запустити API та Web сервери
pnpm dev
```

---

## Альтернатива: Локальна PostgreSQL (якщо Docker не працює)

Якщо не можете запустити Docker, можете використати локальну PostgreSQL:

### Встановлення PostgreSQL

1. Завантажте PostgreSQL 15+ з [офіційного сайту](https://www.postgresql.org/download/windows/)
2. Встановіть PostgreSQL:
   - Порт: `5432` (за замовчуванням)
   - Пароль для користувача `postgres`: `postgres`
3. Створіть базу даних:

```powershell
# Підключитися до PostgreSQL
psql -U postgres

# Створити базу даних
CREATE DATABASE booking;

# Вийти
\q
```

4. Оновіть `apps/api/.env` якщо потрібно:
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/booking?schema=public
```

5. Запустіть міграції:
```powershell
cd apps/api
pnpm prisma:generate
pnpm prisma:migrate
cd ../..
```

6. Запустіть проект:
```powershell
pnpm dev
```

---

## Перевірка статусу Docker

### Команди для перевірки:

```powershell
# Перевірка версії Docker
docker --version

# Перевірка статусу Docker
docker info

# Перевірка запущених контейнерів
docker ps

# Перевірка всіх контейнерів (включно з зупиненими)
docker ps -a

# Перевірка образів
docker images
```

### Очікуваний результат:

Якщо Docker працює, команда `docker ps` покаже щось на кшталт:
```
CONTAINER ID   IMAGE       COMMAND                  CREATED         STATUS         PORTS                    NAMES
abc123def456   postgres:15 "docker-entrypoint.s…"  2 minutes ago   Up 2 minutes   0.0.0.0:5432->5432/tcp   booking_system-postgres-1
```

---

## Часті проблеми

### Проблема 1: Docker Desktop не запускається

**Рішення:**
1. Перезавантажте комп'ютер
2. Перевірте, чи не заблоковано Docker антивірусом
3. Перевірте, чи достатньо ресурсів (RAM, CPU)
4. Спробуйте запустити Docker Desktop від імені адміністратора

### Проблема 2: Порт 5432 вже зайнятий

**Помилка:**
```
Error: bind: address already in use
```

**Рішення:**
1. Знайдіть процес, який використовує порт 5432:
```powershell
netstat -ano | findstr :5432
```

2. Зупиніть процес або змініть порт в `docker-compose.yml`

### Проблема 3: Контейнер не запускається

**Рішення:**
```powershell
# Перевірити логи контейнера
docker logs booking_system-postgres-1

# Перезапустити контейнер
docker restart booking_system-postgres-1

# Або зупинити та запустити знову
pnpm docker:down
pnpm docker:up
```

---

## Швидка перевірка

Виконайте ці команди по черзі:

```powershell
# 1. Перевірка Docker
docker --version
docker ps

# 2. Якщо Docker не працює - запустіть Docker Desktop вручну

# 3. Запуск бази даних
pnpm docker:up

# 4. Перевірка контейнера
docker ps

# 5. Запуск проекту
pnpm dev
```

---

## Додаткова допомога

Якщо проблеми залишаються:

1. Перевірте логи Docker Desktop
2. Перезавантажте Docker Desktop
3. Перезавантажте комп'ютер
4. Перевірте документацію Docker: https://docs.docker.com/desktop/troubleshoot/

