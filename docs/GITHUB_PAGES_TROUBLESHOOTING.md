# 🔍 GitHub Pages Troubleshooting - Детальна діагностика

Якщо workflow "Deploy Web to GitHub Pages" не запускається або падає з помилкою, виконайте наступні кроки:

## Крок 1: Перевірте налаштування GitHub Pages

1. Перейдіть до **Settings** → **Pages** у вашому репозиторії
2. **КРИТИЧНО:** У розділі **Source** має бути вибрано:
   - ✅ **Source**: `GitHub Actions` 
   - ❌ НЕ `Deploy from a branch`
   - ❌ НЕ `main` / `gh-pages`
3. Якщо вибрано інший варіант:
   - Змініть на `GitHub Actions`
   - Збережіть зміни
   - Дочекайтеся завершення налаштування

## Крок 2: Перевірте Environment

1. Перейдіть до **Settings** → **Environments**
2. Переконайтеся, що існує environment з назвою `github-pages`
3. Якщо його немає:
   - GitHub автоматично створить його при першому запуску workflow
   - Або створіть вручну: **Settings** → **Environments** → **New environment** → назва: `github-pages`

## Крок 3: Перевірте Secrets

1. Перейдіть до **Settings** → **Secrets and variables** → **Actions**
2. Переконайтеся, що є secret з назвою `VITE_API_URL`
3. Якщо його немає:
   - Натисніть **New repository secret**
   - **Name**: `VITE_API_URL`
   - **Value**: URL вашого backend API (наприклад, `https://your-api.onrender.com`)
   - Натисніть **Add secret**

**Примітка:** Якщо secret відсутній, workflow все одно має працювати (використає fallback `http://localhost:4000`), але frontend не зможе підключитися до production API.

## Крок 4: Перевірте логи workflow

1. Перейдіть до **Actions** у вашому репозиторії
2. Виберіть failed workflow run (червоний хрестик)
3. Натисніть на job, який упав (зазвичай `build` або `deploy`)
4. Перевірте логи кожного кроку:

### Типові помилки та рішення:

#### Помилка: "Error: Resource not accessible by integration"
**Причина:** Недостатньо permissions або GitHub Pages не налаштований на GitHub Actions.

**Рішення:**
1. Перевірте, що GitHub Pages налаштований на `GitHub Actions` (Крок 1)
2. Перевірте permissions у workflow файлі (мають бути: `pages: write`, `id-token: write`)

#### Помилка: "Error: No such file or directory: dist"
**Причина:** Build не створив папку `dist` або шлях неправильний.

**Рішення:**
1. Перевірте логи кроку "Build" - чи є помилки компіляції?
2. Перевірте, що `apps/web/vite.config.ts` має правильний `outDir: 'dist'`
3. Перевірте, що шлях у "Upload artifact" правильний: `apps/web/dist`

#### Помилка: "Error: Environment 'github-pages' not found"
**Причина:** Environment не створено.

**Рішення:**
1. Перейдіть до **Settings** → **Environments**
2. Створіть environment з назвою `github-pages`
3. Або дочекайтеся, поки GitHub створить його автоматично

#### Помилка: "Error: Process completed with exit code 1" в Build
**Причина:** Помилка під час збірки (TypeScript, залежності, тощо).

**Рішення:**
1. Перевірте логи кроку "Build" - там буде детальна інформація про помилку
2. Спробуйте запустити `pnpm build` локально в `apps/web` - чи працює?
3. Перевірте, що всі залежності встановлені правильно

#### Помилка: "Error: VITE_API_URL is not defined"
**Причина:** Secret відсутній (але це не критично, є fallback).

**Рішення:**
1. Додайте secret `VITE_API_URL` (Крок 3)
2. Або ігноруйте цю помилку, якщо вона не критична

## Крок 5: Запустіть workflow вручну

1. Перейдіть до **Actions** → **Deploy Web to GitHub Pages**
2. Натисніть **Run workflow** (справа вгорі)
3. Виберіть гілку `main`
4. Натисніть **Run workflow**
5. Дочекайтеся завершення (обидва jobs мають бути зеленими ✅)

## Крок 6: Якщо workflow все ще падає

### Варіант A: Перевірте локально

1. Клонуйте репозиторій локально
2. Виконайте:
   ```powershell
   cd apps/web
   pnpm install
   pnpm build
   ```
3. Якщо build не працює локально, виправте помилки
4. Зробіть commit та push

### Варіант B: Перевірте структуру проекту

Переконайтеся, що структура правильна:
```
booking_system/
├── .github/
│   └── workflows/
│       └── deploy-web.yml
├── apps/
│   └── web/
│       ├── package.json
│       ├── vite.config.ts
│       └── src/
└── package.json
```

### Варіант C: Спробуйте спрощений workflow

Якщо нічого не допомагає, спробуйте створити мінімальний workflow для тестування.

## Крок 7: Перевірте результат

Після успішного виконання workflow:

1. Перейдіть до **Settings** → **Pages**
2. Перевірте, що вказано URL: `https://skalevskyi.github.io/booking-system/`
3. Відкрийте URL у браузері
4. Має відкритися React додаток (Login/Register), а не README.md

## Додаткові поради

- **Очистіть кеш браузера** після деплою (Ctrl+Shift+R)
- **Перевірте в режимі інкогніто** - іноді кеш заважає
- **Дочекайтеся 1-2 хвилини** після завершення workflow - GitHub Pages іноді потребує часу для оновлення
- **Перевірте консоль браузера** (F12) - можуть бути помилки завантаження ресурсів

## Проблема: Старий вигляд після деплою (кешування)

Якщо після успішного деплою на сайті все ще відображається старий вигляд:

### Швидке рішення:

1. **Очистіть кеш браузера:**
   - **Chrome/Edge**: `Ctrl+Shift+Delete` → виберіть "Зображення та файли в кеші" → "Очистити дані"
   - **Firefox**: `Ctrl+Shift+Delete` → виберіть "Кеш" → "Очистити зараз"
   - Або використайте **жорстке оновлення**: `Ctrl+Shift+R` (Windows/Linux) або `Cmd+Shift+R` (Mac)

2. **Перевірте в режимі інкогніто:**
   - Відкрийте сайт в режимі інкогніто/приватному режимі
   - Якщо там все працює правильно - проблема в кеші браузера

3. **Перевірте час деплою:**
   - Перейдіть до **Actions** → знайдіть останній успішний workflow
   - Переконайтеся, що він завершився недавно (менше 5 хвилин тому)
   - GitHub Pages може потребувати до 2-3 хвилин для оновлення

4. **Перевірте версію файлів:**
   - Відкрийте DevTools (F12) → вкладка **Network**
   - Оновіть сторінку (F5)
   - Перевірте, чи завантажуються файли з новими hash-іменами (наприклад, `main.abc123.js`)
   - Якщо бачите старі імена файлів - браузер кешує старі версії

### Детальна діагностика:

1. **Перевірте, чи оновився index.html:**
   ```bash
   # Відкрийте DevTools → Network → оновіть сторінку
   # Знайдіть index.html → перевірте заголовок "Last-Modified"
   # Він має відповідати часу останнього деплою
   ```

2. **Перевірте заголовки кешування:**
   - Відкрийте DevTools → Network
   - Клікніть на `index.html` → вкладка **Headers**
   - Перевірте заголовок `Cache-Control`
   - Має бути `no-cache, no-store, must-revalidate` (якщо налаштовано)

3. **Очистіть кеш GitHub Pages (якщо потрібно):**
   - GitHub Pages кешує файли на своїх CDN серверах
   - Іноді потрібно дочекатися до 5-10 хвилин для повного оновлення
   - Або спробуйте додати query-параметр до URL: `?v=2` або `?t=timestamp`

### Технічні деталі:

- **index.html** не має hash в імені, тому браузер може кешувати стару версію
- **Assets (JS/CSS)** мають hash в іменах (наприклад, `main.abc123.js`), тому нові версії завантажуються автоматично
- **Meta-теги** в `index.html` налаштовані на `no-cache` для запобігання кешуванню HTML

### Якщо проблема залишається:

1. **Спробуйте інший браузер** - для виключення проблем з кешем конкретного браузера
2. **Перевірте, чи оновилися файли на GitHub:**
   - Перейдіть до **Actions** → останній workflow → **deploy** job
   - Перевірте логи - чи завантажилися нові файли
3. **Спробуйте примусово оновити через workflow:**
   - Запустіть workflow вручну (Actions → Run workflow)
   - Дочекайтеся завершення
   - Очистіть кеш браузера
4. **Якщо нічого не допомагає:**
   - Видаліть налаштування GitHub Pages (Settings → Pages → Source: None)
   - Дочекайтеся 1 хвилину
   - Знову налаштуйте (Source: GitHub Actions)
   - Запустіть workflow вручну

## Якщо нічого не допомагає

1. Видаліть налаштування GitHub Pages:
   - **Settings** → **Pages** → **Source**: `None`
   - Збережіть
2. Дочекайтеся 1 хвилину
3. Знову налаштуйте:
   - **Settings** → **Pages** → **Source**: `GitHub Actions`
   - Збережіть
4. Запустіть workflow вручну

---

**Важливо:** Завжди перевіряйте логи failed workflow - там буде детальна інформація про помилку!

