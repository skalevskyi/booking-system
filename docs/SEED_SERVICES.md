# 🌱 Створення тестових послуг

Проблема: У списку послуг немає жодної послуги, тому бронювання не працює.

## Рішення: Запустити seed скрипт

### Варіант 1: Через команду (рекомендовано)

```powershell
cd apps\api
pnpm prisma:seed
```

### Варіант 2: Безпосередньо через tsx

```powershell
cd apps\api
pnpm tsx prisma/seed.ts
```

### Варіант 3: Через Prisma Studio (візуально)

```powershell
cd apps\api
pnpm prisma:studio
```

Потім в браузері:
1. Відкрийте таблицю `services`
2. Натисніть "Add record"
3. Створіть послуги вручну

## Що створює seed скрипт

Seed скрипт створює 4 тестові послуги:

1. **Haircut** - 30 хв, $25.00
2. **Haircut & Styling** - 60 хв, $40.00
3. **Beard Trim** - 15 хв, $15.00
4. **Full Service** - 90 хв, $55.00

## Перевірка

Після запуску seed скрипту:

1. Перезапустіть API сервер (якщо він запущений)
2. Перевірте в браузері: `http://localhost:4000/api/services`
3. Має повернутися масив з 4 послугами
4. На frontend (`http://localhost:5173/bookings/new`) мають з'явитися послуги в списку

## Якщо seed не працює

Можна створити послуги через API (потрібен ADMIN акаунт):

```powershell
# 1. Створіть ADMIN акаунт (через реєстрацію з role: ADMIN або через Prisma Studio)

# 2. Увійдіть як ADMIN

# 3. Створіть послуги через API:
# POST http://localhost:4000/api/services
# Headers: Authorization: Bearer <token>
# Body:
{
  "name": "Haircut",
  "description": "Professional haircut service",
  "durationMin": 30,
  "priceCents": 2500,
  "active": true
}
```

Або використайте Swagger UI: `http://localhost:4000/docs`

## Автоматичний запуск seed

Seed скрипт автоматично перевіряє, чи вже є послуги в базі даних. Якщо є - він не створює дублікати.

Для повторного створення послуг (якщо потрібно):
1. Видаліть послуги через Prisma Studio або API
2. Запустіть seed знову

